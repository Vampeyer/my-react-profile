// Function()
// Creates a new Function object. Calling 
// the constructor directly can create 
// functions dynamically but suffers 
// from security and similar 
// (but far less significant) performance 
// issues to eval(). However, unlike eval(),
//  the Function constructor creates
//   functions that
//  execute in the global scope only.

let effy = new Function()
