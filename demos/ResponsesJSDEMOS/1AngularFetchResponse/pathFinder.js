// pathFinder.js
let par = document.getElementById("firstPar")

const newDiv = document.createElement('div');

let path = "https://reqres.in/api/users?page=2"

let justString = path

newDiv.innerText = justString
par.appendChild(newDiv)
